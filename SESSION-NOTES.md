# Note di sessione

## 2026-06-28

### Banner spazi pubblicitari (PR #125 — merge su `main`)
- Restyle `.ad-mini-bar` → card con gradiente gold/coral, overlay griglia (maschera lineare), icona megafono in cerchio bianco, CTA a pillola bordata, X di chiusura.
- Copy adattato in `it` / `en` / `si` (Tamil non ha chiavi `ad.*`).
- Applicato a entrambi gli spazi (`index.html` AD BAR + AD MINI BAR inline).
- Verificato con Playwright: desktop e mobile.

### Menu-bar home + trasparenza (PR #124 — merge su `main`)
- Fix `.site-nav` su home, trasparenza ripristinata.
