#!/usr/bin/env bash
# Bootstrap sessione — VUOTO di proposito.
# Nessun tool viene più installato automaticamente: gonfiavano il contesto (autocompact thrashing).
# Per riattivare un tool: vedi .references/tools-archive.md — solo su richiesta esplicita dell'utente.
exit 0
